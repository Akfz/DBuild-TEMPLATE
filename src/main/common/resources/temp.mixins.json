{
  "required": true,
  "package": "com.template.mixin",
  "compatibilityLevel": "JAVA_17",
  //if you added aslib (if package or class name have 'client' works only on client (same way for server), else delete this
  "plugin": "v.akfz.aslib.mixin.plugin.AsLibMixinPlugin",
  "mixins": [
    "TestMixin"
  ],
  "injectors": {
    "defaultRequire": 1
  }
}